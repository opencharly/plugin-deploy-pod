# 2026.242.0043 — chore(deps): advance sdk to v0.2026242.11 (nil-layer fix)

## Summary

Advances the sdk pin in `candy/plugin-deploy-pod/go.mod` from `v0.2026241.2117` to `v0.2026242.11` to pick up **opencharly/sdk#189** (`fix(deploykit): skip a nil layer in GenerateInitFragments instead of panicking`), which fixes the nil-pointer panic that crashed this plugin during prepare-venue for `check-agentteams-snapshot` (the deploy's candyOrder includes an add_candy the layers map does not carry).

## How tested

```
$ cd candy/plugin-deploy-pod && go build ./...
(exit 0)
```

The fix was also proven live: a locally-built plugin (with the sdk fix via a replace directive) no longer panics in `GenerateInitFragments` — the snapshot bed's deploy-add proceeds past prepare-venue (the bed then fails at a separately-routed overlay issue, opencharly/charly#467).

## Rulebook compliance

- R0: skills loaded (git-workflow, strict-policy, root-cause-analyzer).
- R1: the panic was RCA'd from the live bed run.
- R2: the overlay issue is routed to its named batch (charly#467).
- R3: no duplication (the sdk fix is the canonical one).
- R4: no workarounds.
- R5: no deprecated path.
- R6: git status checked; no force-push.
- R8: no generated artifacts.
- R9: no host install.
- R10: build on the final tree + the live bed-run evidence.

## Change classification

- **Change class:** dependency pin advance (bug fix pickup)
- **Verification gate:** analysed on a live system (build + the live bed-run evidence)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*