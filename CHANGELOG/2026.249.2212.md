# 2026.249.2212 — feat(cutover-c): adopt the member-tree contract — spec v0.2026249.2129 + sdk v0.2026249.2114

## Summary

Cutover C consumer re-pin for the pod deploy target. This plugin parses authoring configs with its OWN embedded spec pin — the whole project loader runs PLUGIN-SIDE over the reverse channel (loaderkit.LoadUnifiedViaExecutor → GateSchemaVersion + ParseDoc, all compiled into this binary) — so its spec pin IS the schema cap a migrated project hits. The pre-wave binary rejected every migrated tree with `config schema 2026.249.2125 is newer than this charly supports`, blocking the charly group-removal PR and the distro bed R10s.

- **spec v0.2026241.1322 → v0.2026249.2129.** RCA: tag v0.2026249.2106 (the group-kind removal: `group` dropped from #ResourceKind, `Deploy.IsGroup` deleted) carries `SchemaVersion = "2026.248.1030"` — pinning it alone would leave the 2125-stamp rejection in place. A direct diff of 2106 vs the existing tag v0.2026249.2129 shows the ONLY delta is the version row (`schema/version.cue` + `spec/version_gen.go` → `2026.249.2125`), bumped by the same cutover precisely so consumers can read migrated trees. 2129 is therefore the settled contract this wave consumes; 2106's surface is strictly contained in it.
- **sdk v0.2026242.913 → v0.2026249.2114**: the member-tree parse/fold (FoldMembers, position-derived `Deploy.Member`, `bed_session` group-root guard already deleted in #224).
- **member_tree_accept_test.go** (new): the in-repo runtime proof (below).

Compile-break migration: NONE needed. An executed sweep of the removed dual-map surface (`IsGroup`, `SortedMemberKeys`, `SortedNestedKeys`, `ValidateDeploymentChildren`, `HasChildren`, `.Children`, `.Members`) over this plugin's Go on main returned ZERO matches before the re-pin — the plugin consumes spec deploy structures (PodConfigSetupRequest, InstallPlan, FleetNode, DeployVolume, …) that survived the cutover unchanged, and its config write path is kind-blind (deploykit.SaveFleetConfig). The semantic consumer WAS the embedded parser cap, fixed by the re-pin.

## How tested

Executed on the final committed tree (`dc49bc4`, clean `git status`):

```
$ go build ./... && go vet ./... && go test ./... -count=1
ok  	github.com/opencharly/plugin-deploy-pod/candy/plugin-deploy-pod	0.207s
?   	github.com/opencharly/plugin-deploy-pod/candy/plugin-deploy-pod/cmd/serve	[no test files]

$ go test -run 'TestMigrated' -v .
=== RUN   TestMigratedTreeStampAcceptedByEmbeddedParser
--- PASS: TestMigratedTreeStampAcceptedByEmbeddedParser (0.00s)
=== RUN   TestMigratedMemberTreeShapeParsesAndFolds
--- PASS: TestMigratedMemberTreeShapeParsesAndFolds (0.00s)
PASS

$ git grep -nE 'IsGroup|SortedMemberKeys|SortedNestedKeys|ValidateDeploymentChildren|HasChildren|\.Children|\.Members' HEAD -- '*.go'; echo $?
1
```

- **Executed sweep (B14(d))**: zero removed-identifier matches on the committed tree (git grep exit 1).
- **CRITICAL RUNTIME CHECK — config-parse surface, proven IN-REPO.** The new test drives THIS plugin's own embedded parser at the new pins: (1) `TestMigratedTreeStampAcceptedByEmbeddedParser` — the embedded cap is now `2026.249.2125`, and `loaderkit.GateSchemaVersion("charly.yml", "2026.249.2125")` (the exact check that produced the pre-wave rejection) PASSES; (2) `TestMigratedMemberTreeShapeParsesAndFolds` — the converted distro-fedora shape (ONE deploy entity: primary `fedora` substrate + in-body `pod` sidecar + deploy-level `fedora-tools` sibling, zero `group:` nodes) parses via `loaderkit.ParseDoc` and folds via `BuildFleetNode` into the member tree, asserted through the settled accessors `HasMembers`/`InSubstrateMembers`/`DeployLevelMembers`/`MemberByName`/`InSubstrate`/`Alongside`. No fixture gap remains in-repo; the charly-side distro bed R10s rerun against this tag as the live-container confirmation.

## Rulebook compliance

- **R0 (skills first)**: git-workflow + strict-policy/R1 discipline applied before the first remediation; no skill surface beyond those was triggered.
- **R1 (RCA every anomaly)**: the schema-cap rejection root-caused to the embedded spec pin BEFORE remediation; the 2106-vs-2129 delta was proven by direct tag diff (only the version row), not assumed.
- **R2**: not defined in the umbrella rulebook; nothing to disposition.
- **R3 (no duplication)**: the runtime proof reuses sdk/spec primitives (`loaderkit.ParseDoc`, `GateSchemaVersion`, spec member-tree accessors) — no policy or parser re-implemented; the docNode helper mirrors loaderkit's own test helper pattern.
- **R4 (no workarounds)**: no sleeps, no hand-pins, no version-string overrides — the cap fix is the sanctioned upstream version row, consumed by re-pin.
- **R5 (delete legacy completely)**: nothing to delete — zero removed-identifier consumers existed here (sweep above); no legacy shims added.
- **R6 (git safety)**: feature branch, no force-push, no hook bypass, no direct main push.
- **R7 (prove the gate)**: build/vet/test `-count=1` pasted above; this standalone plugin repo has no `task verify` — its Go suite IS the verification gate.
- **R8/R9**: not defined in the umbrella rulebook; nothing to disposition.
- **R10 (fresh disposable proof)**: gates re-run AFTER the commit on the final committed tree (`dc49bc4`, clean status), not an edited worktree.
- Submodule discipline: work confined to this repo; no umbrella-root edits, no cross-boundary git ops, PR-only landing.

## Change classification

- **Class**: dependency re-pin (spec + sdk) + consumer proof test; no behavior change beyond the parser cap the contract requires.
- **Verification gate**: `go build ./... && go vet ./... && go test ./... -count=1` + executed identifier sweep + parse/fold runtime proofs, all on the committed tree.
- **Attribution tier**: `fully tested and validated` — changed paths executed live with retained output (the two new tests exercise the exact parse surface that was failing).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*