# 2026.250.1020 — fix: pod rebuild uses the CC-2 deploy add word (not fleet add)

## Summary

Fixes a CC-2 cutover leftover that broke every pod-bed R10 fresh-rebuild gate: `podRebuild` (the `charly update <pod-bed>` path the disposable-bed `update` step routes through) still shelled the RETIRED `charly fleet add` (exit 80) while its own comment said `deploy add` — a doc/code divergence found by RCA of a failing bed run. Now `[]string{"deploy", "add", p.Name}`; the two stale comments aligned.

## How tested

Fresh-rebuild bed runs on two `disposable: true` targets with this branch fed in via the sanctioned override mechanism. The `[update]` fresh-rebuild gate — the exact leg this change fixes — PASSES in both:

```
EVAL BED: check-crabbox-pod — exit 0 (13/13 steps)
  [update] ok 26.5s   (R10 fresh-rebuild gate: deploy-dispatch rebuild -> deploy add -> config -> start)
EVAL BED: check-crabbox-e2e-pod — exit 0 (13/13 steps)
  [update] ok 18.3s   (same leg, with the post-update check-live re-verify green)
```

Pass-1 without this branch FAILED identically at `[update]` with exactly: `charly fleet add check-crabbox-pod --tag ...: charly subcommand exited 80` (update.log in .check/check-crabbox-pod/2026.250.0955). With the fix: `deploy add` executes live, the rebuild completes, both beds go green. Plugin gates: `go build ./...`, `go test ./...`, `gofmt -l .` clean.

## Rulebook compliance
- R1: failure RCA'd from the bed log to the stale argv; fixed at the root.
- R7: bed outputs pasted above (update leg green in both).
- R10: fresh-rebuild disposable proof on the final tree (the update leg is the R10 gate).
- R3: the CC-2 `deploy` surface is the single word; no legacy path reintroduced. R6: branch-per-change PR.
- R2/R4/R5/R8/R9: N/A — one-argv fix + comments.

## Change classification

- **Class:** runtime fix (CC-2 leftover) in a compiled-in plugin. **Verification gate:** fresh-rebuild disposable beds (both exit 0, update leg green) + go gates. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*